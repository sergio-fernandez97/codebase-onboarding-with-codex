# Agent Instructions

## Scope
- Analyze the repository at `./Python` to create onboarding documentation.
- If the user says "repo" without explicitly naming another path, interpret it as `./Python`.

## Findings Output
- Save every requested finding as a Markdown file under `./findings`.
- Use a meaningful filename for each finding that reflects its content (for example: `architecture-overview.md`).
